// maybe i18n here I guess
const ExportNotice = `Calculator is currently in active development. Saved builds might no longer work between updates.`;

const test = {
  EXPORT_NOTICE: ExportNotice,
};

export default test;
